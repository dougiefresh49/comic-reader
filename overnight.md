# Overnight notes — 2026-04-29

Working through the list while user sleeps. This file is the running log.

## Plan
1. Kick off `describe-panels` for issue 1 in the background (~10 min, ~$0.10)
2. Settings volume + playback speed controls (small PR, ships immediately)
3. Test particle/shader effects in the actual reader (not just preview)
4. Test admin audio-library swap flow
5. MP4 export (spec 05)
6. Offline reading (PWA-style download)

Each gets its own branch + PR so anything that's broken can be reverted.

## Decisions log
